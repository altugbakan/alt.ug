---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCollection, getEntry } from "astro:content";

const allBlogPosts = await getCollection("projects");
---

<BaseLayout title="Altuğ's Projects">
  <h1 class="text-center text-4xl font-bold mb-4">Projects</h1>
  {
    allBlogPosts.map((post) => (
      <div class="mb-4">
        <h2 class="text-2xl font-bold mb-2">{post.data.name}</h2>
        <p class="text-gray-500">{post.data.description}</p>
      </div>
    ))
  }
</BaseLayout>
